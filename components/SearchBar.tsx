"use client";

const SearchBar = () => {
    return (
        <form className="search-bar">
            <input type="text" placeholder="Search..." />
        </form>
    )
}

export default SearchBar
